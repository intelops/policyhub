name: Tekton-modules-CI
on:
  push:
    branches:
      - main
    paths:
      - 'genval/modules/cuemod-tektoncd:v0.60.2/**'
    tags:
      - 'v*'
    if: |
      (startswith(github.ref, 'refs/tags/')) &&
      github.ref == 'refs/head/main' &&
      contains(github.event.head_commit.modified, 'genval/modules/cuemod-tektoncd:v0.60.2/**')

permissions:
  contents: write
  security-events: write
  # Optional: allow read access to pull request. Use with `only-new.
  packages: write

jobs:
  push-artifact:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: '1.22'
          cache: false

      - name: inetall genval
        run: go install github.com/intelops/genval@main
            
      - name: Login to Github Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}    

      - name: push-rego-policies
        run: |
          genval artifact push --reqinput ./genval/modules/cuemod-tektoncd:v0.60.2 \
          --dest oci://ghcr.io/intelops/policyhub/genval/tektoncd-cuemods:v0.0.1 \
          --annotations="authors=intelops Inc."
